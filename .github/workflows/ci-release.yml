name: CI & Release

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]

jobs:
  lint-and-test:
    name: Lint & Test
    runs-on: ubuntu-latest
    strategy:
      matrix:
        php-version: ['8.0', '8.1', '8.2', '8.3']

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: ${{ matrix.php-version }}

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Run PHP Syntax Check
        run: php -l world-inspire-email-validation-for-fluent-forms.php

      - name: Run Contract Tests
        run: python3 -m unittest tests/test_email_guard_contract.py

  build-and-release:
    name: Build & Release Package
    needs: lint-and-test
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Package WordPress Plugin Zip
        run: |
          mkdir -p build/world-inspire-email-validation-for-fluent-forms
          cp world-inspire-email-validation-for-fluent-forms.php build/world-inspire-email-validation-for-fluent-forms/
          cp -r data build/world-inspire-email-validation-for-fluent-forms/
          cp readme.txt build/world-inspire-email-validation-for-fluent-forms/
          cp README.md build/world-inspire-email-validation-for-fluent-forms/
          cp LICENSE build/world-inspire-email-validation-for-fluent-forms/
          cd build
          zip -r ../world-inspire-email-validation-for-fluent-forms.zip world-inspire-email-validation-for-fluent-forms
          cd ..
          ls -la world-inspire-email-validation-for-fluent-forms.zip

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          files: world-inspire-email-validation-for-fluent-forms.zip
          generate_release_notes: true
          draft: false
          prerelease: false
